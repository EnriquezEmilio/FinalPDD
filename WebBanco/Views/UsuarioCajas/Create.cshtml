@model WebBanco.Models.UsuarioCaja

@{
    ViewData["Title"] = "Create";
}

<h1>Crear</h1>

<h4>UsuarioCaja</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="id" class="control-label"></label>
                <select asp-for="id" class ="form-control" asp-items="ViewBag.id"></select>
            </div>
            <div class="form-group">
                <label asp-for="num_usr" class="control-label"></label>
                <select asp-for="num_usr" class ="form-control" asp-items="ViewBag.num_usr"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
